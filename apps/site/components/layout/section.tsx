/**
 * Now part of the shared design system, since the emcee app builds its pages
 * from the same shell. Re-exported here so existing call sites are unchanged.
 */
export { Section, SectionHeader } from '@mohdaslam/ui/section'
