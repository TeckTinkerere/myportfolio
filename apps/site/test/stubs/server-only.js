// Stub for the `server-only` package under Jest. See moduleNameMapper in
// jest.config.js — the real package intentionally throws outside a server
// component, which would prevent the content-layer tests from running.
module.exports = {}
